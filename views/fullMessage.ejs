<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Full Message</title>
</head>
<body>
     <% if (post) { %>
    <h3><%= post.name %></h3>
    <p><%= post.message %></p>
    <p><%= post.created_at %></p>
     <% } else { %>
          <p>Post not found</p>
          <% } %>


          <form
          action="/delete-post/<%= post._id %>"
          method="post">
          <button class="btn">Delete</button>
        </form>

        <form action="/feed/update-post/<%= post._id %>"
            method="get">
            <button>Edit</button>
        </form>
</body>
</html>